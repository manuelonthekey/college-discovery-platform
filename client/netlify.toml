[build]
  # Tell Netlify the build command and where the output lives
  command = "npm run build"
  publish = "dist"

# SPA redirect: send all routes back to index.html so React Router works
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
